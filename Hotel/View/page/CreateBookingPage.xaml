<Page x:Class="Hotel.View.page.CreateBookingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Новое бронирование"
      Background="#f8fafc"
      FontFamily="Segoe UI">

    <Page.Resources>
        <!-- Цветовая палитра -->
        <Color x:Key="PrimaryColor">#3B82F6</Color>
        <Color x:Key="PrimaryDarkColor">#2563EB</Color>
        <Color x:Key="PrimaryDarkerColor">#1D4ED8</Color>
        <Color x:Key="BorderColor">#E2E8F0</Color>
        <Color x:Key="TextColor">#1E293B</Color>
        <Color x:Key="TextSecondaryColor">#64748B</Color>

        <!-- Кисти -->
        <SolidColorBrush x:Key="PrimaryBrush"
                         Color="{StaticResource PrimaryColor}" />
        <SolidColorBrush x:Key="PrimaryDarkBrush"
                         Color="{StaticResource PrimaryDarkColor}" />
        <SolidColorBrush x:Key="PrimaryDarkerBrush"
                         Color="{StaticResource PrimaryDarkerColor}" />
        <SolidColorBrush x:Key="BorderBrush"
                         Color="{StaticResource BorderColor}" />
        <SolidColorBrush x:Key="TextBrush"
                         Color="{StaticResource TextColor}" />
        <SolidColorBrush x:Key="TextSecondaryBrush"
                         Color="{StaticResource TextSecondaryColor}" />

        <!-- Стиль для кнопок -->
        <Style x:Key="PrimaryButtonStyle"
               TargetType="Button">
            <Setter Property="Background"
                    Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="BorderBrush"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Padding"
                    Value="12,8" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Margin"
                    Value="0,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="{StaticResource PrimaryDarkBrush}" />
                </Trigger>
                <Trigger Property="IsPressed"
                         Value="True">
                    <Setter Property="Background"
                            Value="{StaticResource PrimaryDarkerBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled"
                         Value="False">
                    <Setter Property="Background"
                            Value="#E2E8F0" />
                    <Setter Property="Foreground"
                            Value="#94A3B8" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для текстовых полей -->
        <Style TargetType="TextBox">
            <Setter Property="Margin"
                    Value="0,5,0,15" />
            <Setter Property="Padding"
                    Value="10" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="BorderBrush"
                    Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="Foreground"
                    Value="{StaticResource TextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          Padding="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused"
                         Value="True">
                    <Setter Property="BorderBrush"
                            Value="{StaticResource PrimaryBrush}" />
                    <Setter Property="BorderThickness"
                            Value="2" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Margin"
                    Value="0,5,0,15" />
            <Setter Property="Padding"
                    Value="8" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="BorderBrush"
                    Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="Foreground"
                    Value="{StaticResource TextBrush}" />
        </Style>

        <!-- Стиль для DatePicker -->
        <Style TargetType="DatePicker">
            <Setter Property="Margin"
                    Value="0,5,0,15" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Padding"
                    Value="8" />
            <Setter Property="BorderBrush"
                    Value="{StaticResource BorderBrush}" />
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="Foreground"
                    Value="{StaticResource TextBrush}" />
        </Style>

        <!-- Стиль для текстовых заголовков -->
        <Style TargetType="TextBlock"
               x:Key="FormLabelStyle">
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Foreground"
                    Value="{StaticResource TextBrush}" />
            <Setter Property="Margin"
                    Value="0,0,0,5" />
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Border Background="White"
                CornerRadius="12"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                Margin="20"
                Padding="30"
                MaxWidth="600"
                HorizontalAlignment="Center"
                VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Новое бронирование"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0,0,0,20"
                           Foreground="{StaticResource TextBrush}" />

                <TextBlock Text="ФИО клиента"
                           Style="{StaticResource FormLabelStyle}" />
                <TextBox x:Name="fullNameBox" />

                <TextBlock Text="Тип номера"
                           Style="{StaticResource FormLabelStyle}" />
                <ComboBox x:Name="roomTypeComboBox">
                    <ComboBoxItem Content="Одноместный" />
                    <ComboBoxItem Content="Двухместный" />
                    <ComboBoxItem Content="Люкс" />
                    <ComboBoxItem Content="Полулюкс" />
                    <ComboBoxItem Content="Апартаменты" />
                </ComboBox>

                <TextBlock Text="Дата заезда"
                           Style="{StaticResource FormLabelStyle}" />
                <DatePicker x:Name="checkInDatePicker" />

                <TextBlock Text="Дата выезда"
                           Style="{StaticResource FormLabelStyle}" />
                <DatePicker x:Name="checkOutDatePicker" />

                <Button Content="Загрузить изображение клиента"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="LoadImage_Click" />

                <Border BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Margin="0,10"
                        HorizontalAlignment="Center">
                    <Image x:Name="clientImage"
                           Height="200"
                           Stretch="UniformToFill"
                           HorizontalAlignment="Center" />
                </Border>

                <Button Content="Забронировать"
                        Style="{StaticResource PrimaryButtonStyle}"
                        FontSize="16"
                        Margin="0,20"
                        Padding="15,10"
                        Click="Book_Click" />
            </StackPanel>
        </Border>
    </ScrollViewer>
</Page>